# Automatically generated by espanso migration tool
# Original file: default.yml

matches:
  - trigger: ";code"
    replace: |-
      ```
      $|$
      ```
  - trigger: ";snip"
    replace: "``` $|$ ```"
  - trigger: ";date"
    replace: "{{mydate}}"
    vars:
      - name: mydate
        type: date
        params:
          format: "%Y-%m-%d"
  - trigger: ";yest"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "date -d '1 day ago' '+%Y-%m-%d'"
  - trigger: ";time"
    replace: "{{mytime}}"
    vars:
      - name: mytime
        type: date
        params:
          format: "%H:%M"
  - trigger: ";pass"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "< /dev/urandom tr -dc A-Za-z0-9 | head -c32"
  - trigger: ";rand"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: >
            < /dev/urandom tr -dc 'A-Za-z0-9!"#$%&'\''()*+,-./:;<=>?@[\]^_`{|}~' | head -c30
  - trigger: ";hi"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "P=('Salut !' 'Bonjour ! 👋' 'Coucou'); echo ${P[RANDOM%${#P[@]}]}"

  - trigger: ";uuid"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
          cmd: "uuidgen"
